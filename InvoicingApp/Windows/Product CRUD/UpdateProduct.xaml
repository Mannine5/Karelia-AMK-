<Window x:Class="InvoicingApp.UpdateProduct"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InvoicingApp"
        mc:Ignorable="d"
        Title="Update Product" Height="200" Width="370"
        KeyDown="WindowKeyDown">

    <Canvas>

        <Label Content="Select Product" Canvas.Left="10" Canvas.Top="10" />
        <Label Content="Product Name" Canvas.Left="10" Canvas.Top="40" />
        <Label Content="Price" Canvas.Left="10" Canvas.Top="65" />
        <Label Content="Description" Canvas.Left="10" Canvas.Top="90" />

        <ComboBox Name="ProductComboBox" 
          Canvas.Left="115" 
          Canvas.Top="12" 
          Height="25" 
          Width="200"
          ItemsSource="{Binding Products}"
          SelectedItem="{Binding SelectedProduct}">

            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ProductId, StringFormat='ID {0}: '}"/>
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBox Name="NameBox" 
                 Canvas.Left="115" 
                 Canvas.Top="45" Width="200" 
                 Text="{Binding SelectedProduct.Name}" 
                 HorizontalAlignment="Center" 
                 VerticalAlignment="Top"/>

        <TextBox Name="PriceBox" 
                 Canvas.Left="115" 
                 Canvas.Top="70" Width="200" 
                 Text="{Binding SelectedProduct.Price, TargetNullValue=''}" 
                 LostFocus="PriceBox_lostFocus"/>
        
        <TextBox Name="DescriptionBox" 
                 Canvas.Left="115" 
                 Canvas.Top="95" Width="200" 
                 Text="{Binding SelectedProduct.Description, UpdateSourceTrigger=PropertyChanged}" 
                 HorizontalAlignment="Center" VerticalAlignment="Top"/>

        <Button Content="Save" Canvas.Left="200" Canvas.Top="120" Width="55" Click="SaveClicked" />
        <Button Content="Cancel" Canvas.Left="260" Canvas.Top="120" Width="55" Click="CancelClicked" />
    </Canvas>
</Window>
